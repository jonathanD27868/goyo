# The URL the site will be built for
base_url = "https://goyo.hahwul.com"
title = "Goyo"
description = "Goyo is Theme"

build_search_index = true
default_language = "en"
generate_feeds = true
generate_sitemap = true

taxonomies = [
    { name = "tags", paginate_by = 10, rss = true },
    { name = "categories", paginate_by = 10 },
]

[markdown]
bottom_footnotes = true

[markdown.highlighting]
theme = "github-dark"

[languages.ko]
generate_feeds = true
build_search_index = true
taxonomies = [
    { name = "tags", paginate_by = 10, rss = true },
    { name = "categories", paginate_by = 10 },
]

[extra]
footer_html = "<div style='display: flex; align-items: center; justify-content: center; gap: 16px; flex-wrap: wrap;'><a href='https://www.hahwul.com' target='_blank' rel='noopener noreferrer' style='display: inline-flex; align-items: center;'><img src='https://www.hahwul.com/images/logo-gray.png' alt='HAHWUL' style='height: 30px' /></a><span style='opacity: 0.6;'>•</span><span>Powered by <a href='https://www.getzola.org' class='font-medium text-base-content/70 hover:text-base-content transition-colors duration-200'>Zola</a> and <a href='https://github.com/hahwul/goyo' class='font-medium text-base-content/70 hover:text-base-content transition-colors duration-200'>Goyo</a></span></div>"
default_thumbnail = "images/default_thumbnail.jpg"
gtag = ""

nav = [
    { name = "Documents", url = "/introduction", type = "url", icon = "fa-solid fa-book" },
    { name = "GitHub", url = "https://github.com/hahwul/goyo", type = "url", icon = "fa-brands fa-github" },
    { name = "Links", type = "dropdown", icon = "fa-solid fa-link", members = [
        { name = "Creator Blog", url = "https://www.hahwul.com", type = "url", icon = "fa-solid fa-fire-flame-curved" },
        { name = "Zola Offical", url = "https://getzola.org", type = "url", icon = "fa-solid fa-z" },
    ] },
]

nav_ko = [
    { name = "문서", url = "/ko/introduction", type = "url", icon = "fa-solid fa-book" },
    { name = "GitHub", url = "https://github.com/hahwul/goyo", type = "url", icon = "fa-brands fa-github" },
    { name = "링크", type = "dropdown", icon = "fa-solid fa-link", members = [
        { name = "제작자 블로그", url = "https://www.hahwul.com", type = "url", icon = "fa-solid fa-fire-flame-curved" },
        { name = "Zola 공식", url = "https://getzola.org", type = "url", icon = "fa-solid fa-z" },
    ] },
]

# Edit this page Configuration
edit_url = "https://github.com/hahwul/goyo/edit/main"

# Language Configuration
[extra.lang]
rtl = [] # List of RTL languages e.g. ["ar", "he"]
aliases = { en = "English", ko = "한국어" } # Language display names for the language selector

# Logo Configuration (new structured format)
# You can use either the new [extra.logo] format or the legacy flat format (logo_text, logo_image_path, logo_image_padding)
# The new format takes priority when both are present
[extra.logo]
text = "Goyo"                           # Text when no logo image
image_path = "images/goyo.png"          # Default logo image path
image_padding = "10px"                 # Optional padding
dark_image_path = "images/goyo-dark.png"   # Logo for dark theme (optional)
light_image_path = "images/goyo-light.png" # Logo for light theme (optional)

# Twitter Configuration (new structured format)
# You can use either the new [extra.twitter] format or the legacy flat format (twitter_site, twitter_creator)
# The new format takes priority when both are present
[extra.twitter]
site = "@hahwul"     # Site Twitter handle
creator = "@hahwul"  # Creator Twitter handle

# Theme Configuration (new structured format)
# You can use either the new [extra.theme] format or the legacy flat format (default_colorset, brightness, disable_theme_toggle)
# The new format takes priority when both are present
[extra.theme]
colorset = "dark"           # Options: "dark", "light"
brightness = "normal"       # Common brightness for both themes: "darker", "normal", "lighter". Only for "goyo-dark" theme
# dark_brightness = "darker"  # Override brightness for dark theme only (optional)
# light_brightness = "normal" # Override brightness for light theme only (optional)
disable_toggle = false      # Hide theme toggle button
default_theme_dark = "goyo-dark"
default_theme_light = "goyo-light"

# Font Configuration (new structured format)
# You can use either the new [extra.font] format or the legacy flat format (custom_font_enabled, custom_font_name, custom_font_path)
# The new format takes priority when both are present
[extra.font]
enabled = false  # Set to true to use custom font
name = ""        # Name of the custom font (e.g., "Roboto", "Noto Sans KR")
path = ""        # Local path (e.g., "fonts/custom.woff") or remote URL (e.g., "https://fonts.googleapis.com/css2?family=Roboto")

# Sidebar Configuration (new structured format)
# You can use either the new [extra.sidebar] format or the legacy flat format (sidebar_expand_depth, disable_root_sidebar_hide)
# The new format takes priority when both are present
[extra.sidebar]
expand_depth = 1        # Sidebar expansion depth (max 5)
disable_root_hide = false # Prevent hiding sidebar on root page
enable_desktop_menu = true # integrate the navigation into the sidebar

# Share Buttons Configuration (new structured format)
# You can use either the new [extra.share] format or the legacy flat format (enable_copy_url, enable_share_x)
# The new format takes priority when both are present
[extra.share]
copy_url = false  # Copy URL button
x = false         # Share on X button

# Favicon Configuration (new structured format)
# Default path is /icons/ for all favicon files
# You can override the base path or individual file paths
[extra.favicon]
base_path = "/icons/"                              # Base path for all favicon files (default: /icons/)
favicon_96x96 = "/icons/favicon-96x96.png"         # Override path for 96x96 PNG favicon
favicon_svg = "/icons/favicon.svg"                 # Override path for SVG favicon
favicon_ico = "/icons/favicon.ico"                 # Override path for ICO favicon
apple_touch_icon = "/icons/apple-touch-icon.png"   # Override path for Apple touch icon
site_webmanifest = "/icons/site.webmanifest"       # Override path for site manifest
